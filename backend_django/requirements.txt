# ===== Dépendances communes (dev + prod) =====
Django>=5.1,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
Pillow>=10.0
requests>=2.31
python-dotenv>=1.0
gunicorn>=21.2
whitenoise>=6.6
psycopg2-binary>=2.9

# MySQL (optionnel, utile en local Windows avec XAMPP/WAMP)
# Installé seulement si la variable INSTALL_MYSQL=1
mysqlclient>=2.2; sys_platform == "win32" or os.environ.get("INSTALL_MYSQL") == "1"
